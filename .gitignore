node_modules/
dist/

# Everything below is staged into public/ from js/poker-session by
# `npm run stage` (which dev and build both run first), so that `vite dev`
# serves it at the site root the same way a production build does. The
# originals are tracked in that package, not here.
public/pokerWorker.bundle.js
public/pokerWorker.bundle.js.map
public/gamecore.js
public/gamecore.wasm
public/cards/
